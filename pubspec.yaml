name: command_history
description: >-
  Generic immutable undo/redo ring for Dart. Type-safe CommandHistory<S>
  with Command<S> interface — no Flutter dependency, framework-agnostic.
version: 0.1.0
repository: https://github.com/RanveerSingh1997/command_history
issue_tracker: https://github.com/RanveerSingh1997/command_history/issues

environment:
  sdk: ^3.9.2

dev_dependencies:
  dart_code_linter: ^3.1.1
  lints: ^4.0.0
  test: ^1.24.0
